[general]
domain=https://videocall-peer.herokuapp.com/
cert=configs/certs/cert.pem
key=configs/certs/key.pem
bind=0.0.0.0
port := os.Getenv("PORT")
if port == "" {
    port = "9909" // Default port if not specified
}
err := grace.Serve(":" + port, context.ClearHandler(http.DefaultServeMux))
html_root=web

[turn]
public_ip=127.0.0.1
port=19302
realm=flutter-webrtc
